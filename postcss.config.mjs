/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // This should be '@tailwindcss/postcss'
    "@tailwindcss/postcss": {},
    autoprefixer: {},
  },
};

export default config;
